import React from 'react';

const SingleCourse = ({ course }) => {
    const { name, img, id, } = course;
    return (
        <div>
            <h1>Single Course</h1>
        </div>
    );
};

export default SingleCourse;